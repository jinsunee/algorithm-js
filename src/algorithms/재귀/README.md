# 재귀호출

아래코드에서 볼 수 있는 것처럼

- 원하는 조건이 아니라면 자기자신을 호출하고 또 호출하는 알고리즘이다.
- 원하는 조건일 때 탈출가능!

```
function 재귀함수(input ...) {
  if (원하는 조건 === true) {
    return;
  }

  재귀함수(input ...);
}
```

## 대표적인 문제

- 피보나치 수열
- 꼬리 재귀
- 파스칼의 삼각형
