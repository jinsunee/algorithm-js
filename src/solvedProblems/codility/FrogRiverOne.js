function solution(X, A) {
  const set = new Set();
   
  for (let i = 0, length = A.length; i < leng; i++) {
    set.add(A[i]);
        
    if (s.size === X) {
      return idx;
    }
  }
    
  return -1;
}